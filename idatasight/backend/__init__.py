"""Backend seam — messages, dispatch stub, and demo stand-in content.

The UI talks to the backend only through hooks.dispatch(msg). Phase 2 wires
that single function into the TAOpy actor system; nothing else changes.
"""
